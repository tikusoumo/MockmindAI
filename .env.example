# --- Core Configuration ---
APP_ENV=dev
PORT=8000
DATABASE_URL=postgresql://app:app@db:5432/voice_agent

# --- LiveKit (Voice Serevr) ---
# For local development with docker-compose, use these defaults:
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# --- AI Model Providers (Cloud) ---
# Fill in the keys for the services you want to use.

# 1. Google Gemini (Free Tier Available) - Recommended for simplicity
# Get key: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=

# 2. Groq (Fast Inference, Free Tier for Llama/Mixtral)
# Get key: https://console.groq.com/keys
GROQ_API_KEY=

# 3. Deepgram (Fast STT/TTS, Free Tier for new users)
# Get key: https://console.deepgram.com/
DEEPGRAM_API_KEY=

# 4. OpenAI (Paid)
OPENAI_API_KEY=

# 5. ElevenLabs (High Quality TTS, Limited Free Tier)
ELEVEN_API_KEY=

# --- Model Selection Configuration ---
# Choose which provider to use for each component.
# Options: google, openai, groq (LLM/STT), deepgram (STT/TTS), elevenlabs (TTS)

LLM_PROVIDER=google
STT_PROVIDER=google
TTS_PROVIDER=google

# Example for "Fast & Free" Stack (Groq + Deepgram):
# LLM_PROVIDER=groq
# STT_PROVIDER=deepgram
# TTS_PROVIDER=deepgram

# --- Database Credentials (Optional override) ---
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=voice_agent
