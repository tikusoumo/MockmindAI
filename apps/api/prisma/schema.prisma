generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id            Int      @id @default(autoincrement())
  name          String
  email         String?  @unique
  password_hash String?
  googleId      String?  @unique
  role          String   @default("user")
  avatar        String   @default("")
  level         String   @default("beginner")
  isVerified    Boolean  @default(false)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  // ---------- Relationships ----------
  sessions      InterviewSession[] 
  posts         CommunityPost[]
  comments      CommunityComment[]
  progress      ProgressStat[]
  scheduled     ScheduledSession[]
  studyPlans    StudyPlan[]
  templates     InterviewTemplate[]
  postLikes     PostLike[]
  notifications Notification[]
  practiceRoutines PracticeRoutine[]
  googleCalendarToken Json?

  @@map("users")
}

model OTP {
  id        Int      @id @default(autoincrement())
  email     String
  code      String
  expiresAt DateTime
  createdAt DateTime @default(now())

  @@map("otps")
}

// -------------------------------------------------------------
// Interview Templates & Execution
// -------------------------------------------------------------

model InterviewTemplate {
  id           String   @id @default(uuid())
  title        String
  description  String
  duration     String
  difficulty   String
  icon         String
  color        String
  type         String   @default("Custom")

  isSystem     Boolean  @default(false)
  creatorId    Int?
  creator      User?    @relation(fields: [creatorId], references: [id], onDelete: SetNull)

  documents    Json     @default("[]") // Array of { name, url, size }
  systemPrompt String?  @db.Text

  isPublished  Boolean  @default(false)
  publishedAt  DateTime?
  usageCount   Int      @default(0)
  rating       Float    @default(0)
  ratingCount  Int      @default(0)

  sessions     InterviewSession[]

  @@map("interview_templates")
}

model InterviewSession {
  id          String   @id @default(uuid())
  
  // Link back to creator/owner
  userId      Int
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  // Optional template reference
  templateId  String?
  template    InterviewTemplate? @relation(fields: [templateId], references: [id])
  
  // ----- Core Details -----
  title       String   @default("Custom Interview Session")
  jobRole     String?  // e.g., "Senior Frontend Developer"
  focusAreas  String?  @db.Text // e.g., "System Design, React, Performance"
  
  // ----- Materials -----
  materials   Json     @default("[]") // Array of { name: 'resume.pdf', url: '...', size: 1024 }
  resumeText  String?  @db.Text       // Extracted text from uploaded materials
  companyInfo String?  @db.Text       // Job Description or Company Details

  // ----- Configuration -----
  accessType  String   @default("link")      // "link" or "restricted"
  type        String   @default("Technical") // E.g., Behavioral, Technical
  difficulty  String   @default("medium")
  aiBehavior  String   @default("learning")  // E.g., "learning" (Hints & Guidance), "strict", "mock"
  persona     String   @default("Sarah")     // E.g., "Sarah (Friendly)", "John (Strict)"
  systemPrompt String?  @db.Text // AI configuration overrides
  
  // ----- Execution State -----
  status      String   @default("pending") // pending, in-progress, completed
  startedAt   DateTime?
  completedAt DateTime?
  duration    Int?     // in seconds

  report      Report?
  participants SessionParticipant[]
  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("interview_sessions")
}

model SessionParticipant {
  id        String   @id @default(uuid())
  
  sessionId String
  session   InterviewSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  
  // Can link to an existing user if registered, otherwise just email
  userId    Int?
  
  email     String
  name      String?
  role      String   @default("Candidate") // "Candidate", "Interviewer", "Observer"
  status    String   @default("invited")   // "invited", "joined", "declined"
  
  createdAt DateTime @default(now())
  
  @@map("session_participants")
}

// -------------------------------------------------------------
// Reporting & Feedback (1:1 with InterviewSession)
// -------------------------------------------------------------

model Report {
  id                  String   @id @default(uuid())
  
  // Direct link to the InterviewSession
  sessionId           String   @unique
  session             InterviewSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  
  date                DateTime @default(now())
  overallScore        Int
  duration            String
  hardSkillsScore     Int
  softSkillsScore     Int
  
  // Stored as JSON arrays/objects
  radarData           Json     @default("[]")
  timelineData        Json     @default("[]")
  fillerWordsAnalysis Json     @default("[]")
  pacingAnalysis      Json     @default("[]")
  behavioralAnalysis  Json     @default("{}")
  swot                Json     @default("{}")
  resources           Json     @default("[]")
  
  questions           QuestionFeedback[]
  transcripts         TranscriptEntry[]

  @@map("reports")
}

model QuestionFeedback {
  id                Int     @id @default(autoincrement())
  
  reportId          String  @map("report_id")
  report            Report  @relation(fields: [reportId], references: [id], onDelete: Cascade)

  question          String  @db.Text
  userAnswerSummary String  @db.Text
  aiFeedback        String  @db.Text
  score             Int
  improvements      Json    @default("[]")
  audioUrl          String?

  @@map("question_feedback")
}

model TranscriptEntry {
  id        Int    @id @default(autoincrement())
  
  reportId  String @map("report_id")
  report    Report @relation(fields: [reportId], references: [id], onDelete: Cascade)

  speaker   String // "user" or "ai"
  text      String @db.Text
  timestamp String

  @@map("transcript_entries")
}

// -------------------------------------------------------------
// Other Entities -> Tied back to User
// -------------------------------------------------------------

model ScheduledSession {
  id          String   @id @default(uuid())
  userId      Int
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  title       String
  description String?  @db.Text
  date        DateTime
  time        String
  interviewer String
  
  status        String   @default("pending")
  googleEventId String?
  reminderSent  Boolean  @default(false)
  isAiSuggested Boolean  @default(false)
  category      String   @default("practice")
  recurrence    String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  routineId     String?
  routine       PracticeRoutine? @relation(fields: [routineId], references: [id], onDelete: SetNull)

  @@map("scheduled_sessions")
}

model ProgressStat {
  id      Int    @id @default(autoincrement())
  userId  Int
  user    User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  label   String
  value   Int
  change  Int
  history Json   @default("[]")

  @@map("progress_stats")
}

model StudyPlan {
  id          String   @id @default(uuid())
  userId      Int
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  
  title       String
  description String?
  status      String   @default("active") // active, completed
  createdAt   DateTime @default(now())

  @@map("study_plans")
}

model CommunityPost {
  id        String   @id @default(uuid())
  
  authorId  Int      @map("author_id")
  author    User     @relation(fields: [authorId], references: [id], onDelete: Cascade)

  title     String   @default("")
  content   String   @db.Text
  timestamp DateTime @default(now())
  tags      Json     @default("[]")

  comments  CommunityComment[]
  postLikes PostLike[]

  @@map("community_posts")
}

model CommunityComment {
  id        Int      @id @default(autoincrement())
  
  postId    String
  post      CommunityPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  
  authorId  Int
  author    User     @relation(fields: [authorId], references: [id], onDelete: Cascade)

  content   String   @db.Text
  createdAt DateTime @default(now())

  @@map("community_comments")
}

model PostLike {
  id        String   @id @default(uuid())
  postId    String
  post      CommunityPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([postId, userId])
  @@map("post_likes")
}

model Notification {
  id        String   @id @default(uuid())
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  type      String
  title     String
  body      String   @db.Text
  read      Boolean  @default(false)
  metadata  Json     @default("{}")
  createdAt DateTime @default(now())

  @@map("notifications")
}

model PracticeRoutine {
  id            String   @id @default(uuid())
  userId        Int
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  title         String
  description   String?  @db.Text
  frequency     String   @default("daily")
  focusAreas    Json     @default("[]")
  duration      Int      @default(30)
  startDate     DateTime
  endDate       DateTime?
  isActive      Boolean  @default(true)
  createdAt     DateTime @default(now())

  sessions      ScheduledSession[]

  @@map("practice_routines")
}
